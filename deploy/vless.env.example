# Config for vk-turn-proxy@vless.service (VLESS / TCP forwarding to Xray)
# Copy to /etc/vk-turn-proxy/vless.env and edit CONNECT.
# Use a different LISTEN port than the udp instance.

LISTEN=0.0.0.0:56001
CONNECT=127.0.0.1:8443
EXTRA_ARGS=-vless
